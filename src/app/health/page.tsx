import { HealthDashboard } from "@/components/health/HealthDashboard";

export const metadata = {
  title: "Health Stats | H.E.A.R.T*",
};

export default function HealthPage() {
  return <HealthDashboard />;
}
